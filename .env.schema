# This env file uses @env-spec - see https://varlock.dev/env-spec for more info
# Runtime provider secrets for the public app are stored in the OS keychain.
# This schema is intentionally minimal and is mainly for lite-server or local tooling.
#
# @defaultRequired=false @defaultSensitive=false
# ----------

# @plugin(@varlock/bitwarden-plugin@0.0.4)
# @initBitwarden(accessToken=$BITWARDEN_ACCESS_TOKEN)

# Optional: only needed when resolving Bitwarden-backed values locally.
# @type=bitwardenAccessToken @sensitive @optional
BITWARDEN_ACCESS_TOKEN=

# Optional: hstack-server-lite falls back to sqlite:hstack_lite.db if unset.
# @type=string @sensitive @optional
DATABASE_URL=bitwarden("67e29d82-8843-4361-bc85-b40f0158b270")
